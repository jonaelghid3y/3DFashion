import React, {useRef} from 'react'
import { Text } from '@react-three/drei'
import { useCostumize } from '../Context/Costumize'
import { gsap } from 'gsap';
import { useEffect } from 'react';

export default function ThreejSText() {

    // useCostumize

 

    
    
    return (
        <>
            
            {/* <Text textAlign="center" position={[-2, -7, 2]} font="./Anurati-Regular.otf" fontSize={1} curveSegments={16} >
                {model === 1 ? 'AERO' : model === 2 ? 'ENTO' : ''}

            </Text> */}
        </>

    )
}
